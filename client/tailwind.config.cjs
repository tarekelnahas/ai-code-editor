/*
 * Tailwind CSS configuration. The editor uses a dark/light neutral palette
 * and extends the default theme only where necessary. Feel free to tweak
 * values to suit your personal taste. Content points to all source
 * locations that should be scanned for class names.
 */
module.exports = {
  content: [
    './index.html',
    './src/**/*.{js,ts,jsx,tsx}'
  ],
  theme: {
    extend: {}
  },
  plugins: []
};